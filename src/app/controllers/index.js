const ClientController = require('./Clients/ClientController');
const ZipCodeController = require('./ZipCode/ZipCodeController');

const controllers = {
  ClientController,
  ZipCodeController,
};

module.exports = controllers;
